#!/bin/bash

# benchmark description
DESCRIPTION="Comparing array concat method to spread operator"

# number of benchmark runs
RUNS=10

# number of iterations in a benchmark run
ITERATIONS=1_000_000

# related benchmarks
RELATED_BENCHMAKRS=("array-concatenation")