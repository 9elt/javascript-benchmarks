#!/bin/bash

# benchmark description
DESCRIPTION="Comparing array concat method to spread operator, see also: [**Big array concatenation**](/benchmarks/big-array-concatenation)"

# number of benchmark runs
RUNS=100

# number of iterations in a benchmark run
ITERATIONS=1_000_000
