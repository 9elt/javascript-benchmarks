#!/bin/bash

# benchmark description
DESCRIPTION="testing JSON methods with objects of different size"

# number of benchmark runs
RUNS=10

# number of iterations in a benchmark run
ITERATIONS=1_000_000

# related benchmarks
RELATED_BENCHMAKRS=()
