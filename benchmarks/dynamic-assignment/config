#!/bin/bash

# benchmark description
DESCRIPTION="Comparing ternary operator dynamic assignment to if statement reassignment"

# number of benchmark runs
RUNS=100

# number of iterations in a benchmark run
ITERATIONS=100_000_000
