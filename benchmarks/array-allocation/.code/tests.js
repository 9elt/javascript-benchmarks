/**
 * the code in this file will run after every snippet 
 * and won't be measured during the benchmark
 */
if (array.length != size) {
    throw new Error(`size: ${size} length: ${array.length}`)
}
